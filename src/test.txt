import "/training" as trainingData;
import "/testing" as testingData;

import MNIST as model;

func trainModel(model, data) -> {
    epoch = 0;
    loop {
        accuracy, loss = model(data, SGD);
        if accuracy >= 0.95 {
            break;
        }
        epoch = epoch + 1;
    }
    return model;
}

func testModel(model, data) -> {
    accuracy, loss = model(data);
    return accuracy;
    /*
    comment her og der 
    */
}

m1 = Sigmoid -> 10; //test comment hehehehehe
m2 = ReLU -> 20; //hehehehe
m3 = m1 -> m2;

trainedModel = trainModel(m3, trainingData);

accuracy = testModel(trainedModel, testingData);

// et eller andet output her msske
// print maske eller noget (error fordi det er end of file og der er ikke en newline :())